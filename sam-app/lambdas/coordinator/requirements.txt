# requirements.txt — Coordinator Lambda
# =====================================
# These are imported at Lambda runtime; the actual packages live
# in the DependenciesLayer (layers/dependencies/).
# Keep this file in sync with layers/dependencies/requirements.txt.
#
# Lambda Layer handles the install — this file is for reference / IDE support.

langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langchain-anthropic>=0.3.0,<0.4.0
langgraph>=0.2.0,<0.3.0
openai>=1.50.0,<2.0.0
anthropic>=0.36.0,<1.0.0
elevenlabs>=1.13.0,<2.0.0
requests>=2.32.0,<3.0.0
httpx>=0.27.0,<1.0.0
pydantic>=2.9.0,<3.0.0
typing_extensions>=4.12.0
