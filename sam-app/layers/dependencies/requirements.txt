# requirements.txt — Lambda Layer (DependenciesLayer)
# =====================================================
# SAM builds this layer by running:
#   pip install -r requirements.txt -t python/
# inside the layers/dependencies/ directory.
#
# The `python/` subfolder maps to /opt/python in Lambda,
# which is automatically on sys.path.
#
# Pin minor versions to prevent unexpected breakage.
# Re-run `sam build` after changing this file.

# LangChain ecosystem
langchain==0.3.25
langchain-core>=0.3.63
langchain-openai==0.2.14
langchain-anthropic==0.3.15
langgraph==0.2.73

# LLM provider clients
openai==1.68.2
anthropic>=0.52.0
tiktoken==0.9.0

# HTTP
requests==2.32.3
httpx==0.28.1
httpcore==1.0.7

# Data / utilities
pydantic==2.11.1
pydantic-core==2.33.0
typing_extensions==4.13.1
tenacity==9.0.0
aiohttp==3.11.14

# ElevenLabs SDK
elevenlabs==1.54.0

# Misc runtime deps
anyio==4.9.0
sniffio==1.3.1
distro==1.9.0
certifi==2025.1.31
charset-normalizer==3.4.1
idna==3.10
urllib3==2.3.0
